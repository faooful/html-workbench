{
  "name": "plan-viewer",
  "version": "1.0.0",
  "main": "main.js",
  "scripts": {
    "start": "electron .",
    "bundle": "esbuild scripts/bundle-diffs.js --bundle --format=iife --global-name=PierreDiffs --platform=browser --outfile=renderer/pierre-diffs.bundle.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "Local-first review inbox and plan memory for AI-generated implementation plans.",
  "dependencies": {
    "chokidar": "^5.0.0",
    "electron": "^41.3.0"
  },
  "devDependencies": {
    "@pierre/diffs": "latest",
    "esbuild": "latest"
  }
}
